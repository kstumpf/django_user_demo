{% extends "registration/base.html" %}
{% load bootstrap3 %}

{% block title %}Sign in{% endblock %}

{% block content %}
<div class="container">
  <div class="row">
    <div class="col-sm-offset-2 col-sm-10">
      <h1>Sign in</h1>
      <p>Not yet registered?  
        <a href="/accounts/register/">Register now for free!</a></p>
      <p>Trouble signing in?  
        <a href="/password/reset/">Reset password.</a></p>
    </div>
  </div>

  {% if form.errors %}
  <div class="row">
    <div class="alert alert-danger col-sm-offset-2 col-sm-6">
      <p><strong>Login failed. Try again...</strong></p>
    </div>
  </div>
  {% endif %}

  <form action="/accounts/auth/" 
      method="post" 
      role="form" 
      class="form-horizontal">
    {% csrf_token %}
      <label for="username">User name: </label>
      <input type="text" name="username" value="" id="username">
      <label for="password">Password: </label>
      <input type="password" name="password" value="" id="password"> 
    {% buttons %}
    <div class="col-sm-offset-2 col-sm-10">
      <button type="submit" class="btn btn-primary">
        {% bootstrap_icon "star" %} Login
      </button>
    </div>
    {% endbuttons %}
  </form>

</div>
{% endblock %}
