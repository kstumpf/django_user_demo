{% extends "registration/base.html" %}
{% load url from future %}
{% load bootstrap3 %}

{% block title %}{{ post.title }}{% endblock %}

{% block content %}
    <div class="container">
      <div class="jumbotron">
        <div class="container">
	  <h2>{{ post.title }}</h2>
	  {{ post.content|linebreaks }}
	  <p>{{ post.updated_at }}</p>
	  <a class="btn btn-lg btn-info"
             href="{% url 'microblog:list' %}">Blog List</a>
	  {% if user.is_authenticated %}
          <a class="btn btn-lg btn-info"
	     href="{% url 'microblog:edit' post.pk %}">Edit Post</a>
	  {% endif %}
        </div> <!-- /.container -->
      </div> <!-- /.jumbotron -->
    </div> <!-- /.container -->
{% endblock content %}
