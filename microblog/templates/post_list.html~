{% extends "registration/base.html" %}
{% load url from future %}
{% load bootstrap3 %}

{% block title %}Blog Posts{% endblock %}

{% block content %}
    <div class="container">
      <div class="jumbotron">
        <div class="container">
          <h1>Blog Posts</h1>

	  <h3>Language is: {{language}}</h3>

	  <h3>Session Language is: {{session_language}}</h3>

          <p class="lead">Below you will find all of the posts.</p>
	  <p><ul>
	    {% for post in post_list %}
	    <li><a href="{% url 'microblog:detail' post.pk %}">{{ post.title }}</a></li>
	    {% empty %}
	    <li>Sorry, no posts yet. Check back soon!</li>
	    {% endfor %}
          </ul>
	  {% if user.is_authenticated %}
	  <a class="btn btn-lg btn-info"
             href="{% url 'microblog:create' %}">Create New Post</a>
	  {% endif %}
	  <h3>Search</h3>
	  {% csrf_token %}
	  <input type="text" id="search" name="search" />
	  <!-- Display li tags we're generating in ajax search -->
	  <ul id="search-results">
	    
	  </ul> 

	  </p>
        </div> <!-- /.container -->
      </div> <!-- /.jumbotron -->
    </div> <!-- /.container -->
{% endblock content %}
